[project]
name = "pypi-publish"
dependencies = [
    "python (>=3.11)",
    "cryptography (==43.0.3)",
    "google-cloud-secret-manager (==2.22.0)",
    "osv (==0.0.22)",
    "requests (==2.32.3)",
]

[tool.poetry]
package-mode = false

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
