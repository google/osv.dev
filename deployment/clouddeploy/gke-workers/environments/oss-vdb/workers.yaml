apiVersion: apps/v1
kind: Deployment
metadata:
  name: workers
spec:
  template:
    spec:
      containers:
      - name: worker-private
        env:
          - name: GOOGLE_CLOUD_PROJECT
            value: oss-vdb
        args:
          - "--vulnerability-bucket=osv-vulnerabilities"
          - "--notify-pypi=true"
